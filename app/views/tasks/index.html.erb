<h1>Tasks:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><% if task.completed == true %>
      <i class="far fa-check-square"></i>
    <% else %>
      <i class="far fa-square"></i>
    <% end %>
    <%= link_to task.title, task_path(task) %><span> | </span><%= link_to '✕', task_path(task), method: :delete %></li>
  <% end %>
</ul>

<%= link_to 'add', new_task_path %>
